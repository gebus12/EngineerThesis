@model EngineerCodeFirst.Models.Bus

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Bus</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.RegNum)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.RegNum)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Status)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Status)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.GeoLocation)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.GeoLocation)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Accessible)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Accessible)
        </dd>

        

    </dl>
    <table class="table">
        <tr>
            <th>
                Line
            </th>
        </tr>

        @foreach (var item in Model.Lines)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.LineInfo)
                </td>
            </tr>
        }
    </table>
    <table class="table">
        <tr>
            <th>
                Driver
            </th>
        </tr>

        @foreach (var item in Model.Drivers)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.DriverInfo)
                </td>
            </tr>
        }
    </table>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
